url = ${?URL}
users = ${USERS}
atOnceUsers = ${AtOnceUsers}
rampUsers = ${RampUsers}
rampFrequency = ${RampFrequency}
constantUsers = ${ConstantUsers}
constantUsersFrequency = ${ConstantUsersFrequency}
rampUsersPerSecond = ${RampUserStartingRate}
rampUsersPerSecondTarget = ${RampUserTargetRate}

