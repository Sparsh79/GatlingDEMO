platform: linux

image_resource:
  type: docker-image
  source:
    repository: alpine
    tag: latest

inputs:
  - name: gatling
outputs:
  - name: sbt-output

run:
  path: /bin/sh
  args:
    - gatling/run.sh